# 🔄 Git Workflow для RoyalPark

## 📋 Структура веток

```
main          → Продакшн (только релизы)
develop       → Интеграционная ветка (все фичи)
feature/user  → Ветка для работы над user-частью
feature/admin → Ветка для работы над admin-частью
```

---

## 🚀 Быстрый старт

### Первоначальная настройка (один раз)

```bash
# Запустите скрипт настройки
scripts\git-setup.bat
```

Или вручную:

```bash
git checkout main
git pull origin main
git checkout -b develop
git push -u origin develop
```

---

## ✅ Ежедневный workflow

### 1️⃣ Перед началом работы — ОБНОВИТЬ

**Вариант А: Используйте скрипт (рекомендуется)**

```bash
# Для работы над user-частью
scripts\git-pull.bat feature/user

# Для работы над admin-частью
scripts\git-pull.bat feature/admin
```

**Вариант Б: Вручную**

```bash
# Переключитесь на свою ветку
git checkout feature/user  # или feature/admin

# Обновите develop
git checkout develop
git pull origin develop

# Вернитесь на свою ветку и обновите её
git checkout feature/user
git pull origin feature/user
```

---

### 2️⃣ Работайте над задачей

Делайте изменения, коммитьте часто:

```bash
git add .
git commit -m "Описание изменений"
```

---

### 3️⃣ После завершения работы — PUSH

**Вариант А: Используйте скрипт (рекомендуется)**

```bash
scripts\git-push.bat feature/user "Update user dashboard"
```

**Вариант Б: Вручную**

```bash
# Обновите перед пушем
git pull origin feature/user

# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Отправьте в свою ветку
git push origin feature/user
```

---

## 🔀 Слияние через Pull Request

### Когда ваша работа готова:

1. **Зайдите на GitHub**: https://github.com/vironsided/RoyalPark

2. **Создайте Pull Request**:
   - Нажмите "Pull requests" → "New pull request"
   - **From**: `feature/user` (или `feature/admin`)
   - **Into**: `develop`
   - Добавьте описание изменений
   - Нажмите "Create pull request"

3. **Дождитесь ревью** (если нужно)

4. **После мержа**:
   ```bash
   git checkout develop
   git pull origin develop
   git checkout feature/user
   git pull origin develop  # Обновить свою ветку
   ```

---

## ⚠️ Решение конфликтов

Если при `git pull` появились конфликты:

1. **Git покажет файлы с конфликтами**

2. **Откройте файлы** и найдите маркеры:
   ```
   <<<<<<< HEAD
   Ваш код
   =======
   Код из develop
   >>>>>>> develop
   ```

3. **Решите конфликт** вручную, оставив нужный код

4. **Сохраните файл** и выполните:
   ```bash
   git add .
   git commit -m "Fix merge conflict"
   git push origin feature/user
   ```

---

## 📝 Правила работы

### ✅ ДЕЛАЙТЕ:

- ✅ Всегда `pull` перед `push`
- ✅ Работайте только в своей ветке (`feature/user` или `feature/admin`)
- ✅ Делайте маленькие коммиты с понятными сообщениями
- ✅ Создавайте Pull Request для слияния в `develop`
- ✅ Обновляйте свою ветку после мержа в `develop`

### ❌ НЕ ДЕЛАЙТЕ:

- ❌ Не пушите напрямую в `main` или `develop`
- ❌ Не работайте вдвоём в одной ветке
- ❌ Не игнорируйте конфликты
- ❌ Не делайте `force push` (git push --force)

---

## 🛠️ Полезные команды

### Переключение между ветками

```bash
# Используйте скрипт
scripts\git-switch.bat feature/user

# Или вручную
git checkout feature/user
```

### Просмотр изменений

```bash
# Что изменено
git status

# Детали изменений
git diff

# История коммитов
git log --oneline --graph --all
```

### Отмена изменений

```bash
# Отменить изменения в файле (до git add)
git restore filename

# Отменить git add
git restore --staged filename

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1
```

---

## 🎯 Примеры сценариев

### Сценарий 1: Начало нового дня

```bash
# 1. Обновить свою ветку
scripts\git-pull.bat feature/user

# 2. Начать работу
# ... делаете изменения ...

# 3. Закоммитить
git add .
git commit -m "Add new feature"

# 4. Отправить
scripts\git-push.bat feature/user "Add new feature"
```

### Сценарий 2: После мержа PR в develop

```bash
# 1. Обновить develop
git checkout develop
git pull origin develop

# 2. Обновить свою ветку
git checkout feature/user
git pull origin develop

# 3. Продолжить работу
```

### Сценарий 3: Конфликт при обновлении

```bash
# 1. Попытка обновления
git pull origin develop

# 2. Git показывает конфликт
# 3. Решаете конфликт вручную в файлах
# 4. Сохраняете файлы

# 5. Завершаете мерж
git add .
git commit -m "Resolve merge conflict"
git push origin feature/user
```

---

## 📞 Если что-то пошло не так

### Проблема: "Your branch is behind"

```bash
git pull origin feature/user
```

### Проблема: "Cannot push, remote has changes"

```bash
git pull origin feature/user
# Решите конфликты если есть
git push origin feature/user
```

### Проблема: "Branch not found on origin"

```bash
# Первый пуш новой ветки
git push -u origin feature/user
```

---

## 🔗 Полезные ссылки

- GitHub репозиторий: https://github.com/vironsided/RoyalPark
- Создать Pull Request: https://github.com/vironsided/RoyalPark/compare
- Просмотр веток: https://github.com/vironsided/RoyalPark/branches

---

**Удачной работы! 🚀**

