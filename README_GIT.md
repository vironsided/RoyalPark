# 🚀 Быстрая инструкция по Git для двух разработчиков

## ⚡ Быстрый старт

### 1. Первая настройка (один раз)

```bash
scripts\git-setup.bat
```

### 2. Ежедневная работа

**Перед началом работы:**
```bash
scripts\git-pull.bat feature/user    # если работаешь над user
# или
scripts\git-pull.bat feature/admin   # если работаешь над admin
```

**После завершения работы:**
```bash
scripts\git-push.bat feature/user "Описание изменений"
```

---

## 📋 Структура веток

- **`main`** - продакшн (только релизы)
- **`develop`** - интеграция всех фич
- **`feature/user`** - твоя ветка для user-части
- **`feature/admin`** - ветка для admin-части

---

## ✅ Правила

1. **Всегда `pull` перед `push`**
2. **Работай только в своей ветке**
3. **Слияние в `develop` только через Pull Request на GitHub**

---

## 🔀 Создание Pull Request

1. Зайди на: https://github.com/vironsided/RoyalPark
2. Нажми "Pull requests" → "New pull request"
3. Выбери: **from** `feature/user` → **into** `develop`
4. Создай PR и дождись мержа

---

## ⚠️ Если конфликт

```bash
git pull origin develop
# Реши конфликт в файлах вручную
git add .
git commit -m "Fix merge conflict"
git push origin feature/user
```

---

**Подробная инструкция:** см. `GIT_WORKFLOW.md`

